server:
  port: 8888

spring:
  application:
    name: config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Gowshika93/Loan-Approval-System
          default-label: main
          clone-on-start: true